# BAYBENCH — detector miss triage log (Phase 4)

The iterate worklist. Each row is a case the detector got wrong (from `bench coverage <tool>` gap list, or a verdict flip in the report). One-line triage per miss; the fix is a predicate change, then rerun `bench run`.

Triage buckets (why the miss happened):
- **placement** — the trigger sits somewhere the detector didn't model (pre-`_transfer` hook, modifier, helper).
- **naming** — name-specific matching missed a name-agnostic role/var (see research §5.1).
- **helper depth** — the gate is reached through N call hops the detector didn't follow (Tokeer-style transfer-path set).
- **external gate** — behavior depends on an unresolved external call → should be `Uncertain(external_dependency)`, not Benign.
- **policy** — finding fired but verdict policy mapped it wrong (e.g. HIGH on a benign OZ-Pausable-with-unpause shape → false positive; needs the §6 discriminator).
- **reporting** — verdict right, output wrong (non-deterministic text, missing evidence fields).
- **compile** — file did not compile with the picked solc; see the retry ladder in `detector/compile.py`.
- **doctrine** — verdict differs from the label's *preferred* value but is inside `accepted_verdicts` by design (concealment doctrine: disclosed/bounded privilege → `Uncertain`). Recorded, not fixed.

Run 1 — 2026-09-19, `bench run detector --no-docker --tier 1 --tier 3` (Tier 0 empty, Tier 2 pending compile ladder): weighted 0.8919, Tier 1 rule recall 1.0, HIGH-FP 0.0 on Tier 1 and Tier 3, evidence hit 1.0, determinism **fail**.

| date | tool | case id | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-19 | detector | tier1/DRAIN_APPROVAL_PULL/ben | Benign | Uncertain(slither_high) | policy | Slither `unchecked-transfer` (High impact, code-quality) lifted the verdict. Overlay lift restricted to exploit-shape checks (`reentrancy-eth`, `arbitrary-send-*`, `suicidal`, `controlled-delegatecall`, `delegatecall-loop`, `msg-value-loop`, `unprotected-upgrade`, `protected-vars`, `rtlo`); other High checks stay INFO evidence (`evidence_only`). | fixed (run 2: Benign) |
| 2026-09-19 | detector | tier1/LEAK_PRIV_SWEEP/ben | Benign (acc. Uncertain) | Uncertain(slither_high) | policy | same `unchecked-transfer` lift; `foreign_only` already downgraded LEAK_PRIV_SWEEP to INFO | fixed (run 2: Benign) |
| 2026-09-19 | detector | (all, `--repeat 2`) | deterministic | run0/run1 differ | reporting | overlay reasoning embeds Slither's description with the staging path (`.bench_work/detector/run0/...`); strip path fragments, keep `#lines` | fixed (run 2: determinism pass) |

Run 2 — same tiers after the overlay fix: weighted 0.8981, determinism pass. Remaining `score < 1` rows are all the doctrine rows below.

Tier 2 compile triage (250 of 779 sources did not compile at ingest; first solc error bucketed, then re-measured through the retry ladder):

| date | tool | case id | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-19 | detector | tier2/* 42 files, exact pragma (`0.4.25`, `0.8.19`, `0.5.16`, …) | compile | `requires different compiler version` | compile | ladder rung (a): relax pragma, same picked minor; 35 rescued, 6 broken underneath, 1 fixed by constraint-aware pick | fixed (35/42) |
| 2026-09-19 | detector | tier2/* 33 files, no pragma (2017-era) | compile | 0.8.20 parser error | compile | ladder rung (b): no pragma → try 0.4.26, 0.5.17, 0.6.12, 0.7.6; 27 rescued, 6 syntax-broken at every 0.4.x (verified on 0.4.11/0.4.19 too) | fixed (27/33) |
| 2026-09-19 | detector | tier2/crpwarner 7 files, duplicate SPDX | compile | `Multiple SPDX license identifiers` | compile | ladder rung (c) removes the error, but all 7 then hit missing imports (partial flattening) | unfixable |
| 2026-09-19 | detector | tier2/pied-piper 171 `injected_*` | compile | undeclared identifier / modifier without `_` / bool→uint | compile | dataset injection produced non-compiling code; no compiler version accepts it | unfixable (dataset) |
| 2026-09-19 | detector | tier2/* 16 files, missing imports / garbage | compile | `Source "…" not found` | compile | dependency files absent from the dataset | unfixable (dataset) |

Net: 529 → 592 compiling of 779 (76%); the 187 remaining are `Uncertain(compile_failed)` by construction and are labelled `Uncertain` in the corpus.

Run 3 — Tier 2 first pass (779 cases, 590 compiling): weighted 0.7901; family recall A 0.967 / B 0.490 / C 1.0 (n=3) / F 0.031 (322); on the 527 compiling Malicious-labelled files 368 Malicious, 97 Uncertain, 62 Benign.

Run 4 — 2026-09-20, Tier 2 after the root-cause pass (all fixes in the analysis layer + `HONEYPOT_LEGACY`; each with a regression test in `tests/detector/test_tier2_regressions.py`): weighted 0.7901 → **0.8209**; family recall 0.291 → **0.917** (A 0.978 / B 0.878 / C 1.0 / F 0.910); 373 Malicious / 141 Uncertain / **13** Benign on the compiling Malicious-labelled files. Per HoneyBadger technique (hits/compiling): balance_disorder 17/17, hidden_state_update 138/157, hidden_transfer 11/11, inheritance_disorder 40/45, named_* 24/27, skip_empty_string_literal 7/7, straw_man_contract 29/29, type_deduction_overflow 4/4, uninitialised_struct 23/24. Tier 1/3 per-case output unchanged (rule recall 1.0, determinism pass).

| date | tool | case | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | detector | tier2: 157 files hidden_state_update + 17 balance_disorder | F hit | 0 hits (silent `if` not a gate; `keccak256(arg)` untraced; `balance+msg.value` not whole-pot) | placement | `guards_of_node`, `else_guarded_nodes`, `values_feeding_condition` through SolidityCall, `pot_dependent`, `_send_gates`, `>=`/`<=` disorder | fixed (138/157, 17/17) |
| 2026-09-20 | detector | tier2: hidden_state_update writers (`SetPass`/`StartRoulette`/`PutGift` shapes) | F hit | state var looked depositor-writable | policy | `depositor_locked` (privileged or silently state/`msg.value`-gated writers), `bait_writer_exists`, `_priv_settable` (quiz reset), `_caller_vs_locked_compare` | fixed |
| 2026-09-20 | detector | tier2: `balance_disorder_0x5bb5…` | F hit | analysis crash (`RecursionError` in `root_state`) | placement | `_seen` cycle guard in `root_state` | fixed |
| 2026-09-20 | detector | tier2: 45 files inheritance_disorder | F hit | `shadowing-state` evidence only; silent `if (msg.sender==Owner) _;` not an auth gate; `isOwner()` bool helper no atom | helper depth / naming | `shadowed_auth_vars` + `_shadowed_role_hits`; `_guards_placeholder` end node; `_bool_helper_atoms` | fixed (40/45) |
| 2026-09-20 | detector | tier2: 29 files straw_man_contract | F hit | external call on state address not tied to the ETH path; delegatecall variant; param destination | placement | `state_target_calls`, `_straw_man` with `_caller_controlled` | fixed (29/29) |
| 2026-09-20 | detector | tier2: 11 files hidden_transfer | F hit | LEAK_PRIV_SWEEP only; function looked privileged via nested owner check | placement | `_double_pot` runs before `is_privileged` | fixed (11/11) |
| 2026-09-20 | detector | tier2: 24 uninitialised_struct, 4 type_deduction_overflow, 7 skip_empty_string_literal | F hit | no compiler-quirk triggers | placement | `_uninitialised_storage` (incl. modifiers, `selfdestruct` exits), `_narrow_loop_counter`, `_empty_string_call` (0.4 only) | fixed (23/24, 4/4, 7/7) |
| 2026-09-20 | detector | tier2: `hidden_state_update_0x11f4…` SimpleBet, `0x3c3f…` + `named_EtherBet` | F hit | fixed 2-ether prize behind silent `!locked` | placement | `_locked_switch_gate` (payable, fixed prize, depositor-locked bool with a writer; pausable `msg.value` refund excluded) | fixed |
| 2026-09-20 | detector | tier2: `inheritance_disorder_0xf561…` BankOfStephen | F hit | Benign (`owner['Stephen']` vs writer `owner['Stephеn']`) | naming (homoglyph key) | `_constant_key_slot` + `_slot_never_rewritten` + payable writer | fixed |
| 2026-09-20 | detector | tier2: 37 files pied-piper injected_DestroyToken | B hit | no ledger bound (no ERC-20 ABI) | placement | `_paired_ledger_fallback` in `bind()` | fixed |
| 2026-09-20 | detector | tier2: `real_0x9f8f…` MKR (DSAuth) | B hit / Malicious | Benign; `mint` not privileged | helper depth | sender taint through `isAuthorized(src,…)`, `_bool_helper_atoms` | fixed (MED via `issuer_token`) |
| 2026-09-20 | detector | tier2: crpwarner `0xD217…` BaseToken (OZ 3.x AccessControl) | B hit | `hasRole` → `EnumerableSet.contains` gave no atom | helper depth | `_membership_call_atoms` (set membership over a state-rooted arg keyed by sender = `map_bool`) | fixed |
| 2026-09-20 | detector | tier2: 6 files crpwarner `0x51C5…`, `0x8275…`, `0x94b7…`, `0xAAf8…`, `0xEc4C…`, `0xf0b6…` | Malicious (manifest note empty) | Benign | dataset | plain OZ ERC-20s / GnosisSafeProxyFactory, no privileged ledger or exit function | recorded |
| 2026-09-20 | detector | tier2: crpwarner `0x11CB…` DHE | Malicious | Benign | policy | `_isSniper` gate also has an unprivileged writer (auto-flag in `_transfer`), so `privileged_writable` rejects it although `removeSniper(a,true)` lets the owner blacklist anyone. Spec amended: a gate is privileged-writable when **any** privileged writer can store the blocking polarity | open |
| 2026-09-20 | detector | tier2: `real_0x6e8b…` CalorieCoin | A | Benign | dataset | `frozenAccount` written by owner but never read on any transfer path (dead flag) | recorded |
| 2026-09-20 | detector | tier2: `injected_FreezeAccount_8` | A | Benign | dataset | file is the bare SWC overflow sample; no freeze code injected | recorded |
| 2026-09-20 | detector | tier2: `hidden_state_update_0x07ec…` TitsCrypto | F | Benign | dataset | whole-pot sweep to a token-holder-voted address (owner proposes); governance shape, no structural trap | recorded |
| 2026-09-20 | detector | tier2: `inheritance_disorder_0x9c0c…` EtherToTheMoon | F | Benign | policy | owner `withdraw(_to,_amount)` bounded by total deposits — parametrised privileged drain; LEAK_PRIV_SWEEP required `address(this).balance`. Spec amended: amount may be a parameter/state value not bounded to the caller's own ledger entry | open |
| 2026-09-20 | detector | tier2: `inheritance_disorder_0xfdc3…` DoubleETH | F | Benign | placement | inverted modifier (`require(msg.sender != owner)`) and a deposit ledger with no exit path; no rule models "no depositor exit" | open (needs new predicate) |
| 2026-09-20 | detector | tier2: `named_TwelHourTrains` | F | Benign | placement | time-weighted Ponzi; PONZI_SHAPE needs a payable-populated payee store | open (low) |
| 2026-09-20 | detector | tier2: 17 files hidden_state_update (EthTeamContract×6, TeamToken×2, Controller×2, Lottery/REV/POOH×3, PassDao×2, Wolk, DrainMe) + `0xff41…` ERC721 | F | Malicious via LEAK_PRIV_SWEEP / OWN_FAKE_RENOUNCE / FEE_UNBOUNDED / EXIT_TIME_GATE / BAL_* | dataset | HoneyBadger technique tag on token rug-pulls; verdict correct, malice is A/B/C/D-shaped | recorded |
| 2026-09-20 | detector | tier2: `named_X2_FLASH` | F | Malicious via STRUCT_SELFDESTRUCT | placement | `msg.sender.call.value(this.balance);` without `()` — no call in IR (0.4 no-op send) | open |
| 2026-09-20 | detector | tier2: `uninitialised_struct_0x7480…` CryptoRoulette, `0x0320…` TitsToken, `0x2d05…` Etherwow, `0x8490…` SimpleDeposit | F | Malicious via LEAK_PRIV_SWEEP / STRUCT_SELFDESTRUCT | dataset | owner-only sweep / selfdestruct is the actual malice | recorded |
| 2026-09-20 | detector | tier2: `named_RACEFORETH`, `hidden_state_update_0x7409…` PhoenixLite | F | Malicious / Uncertain | placement | arithmetic trap (halving limit never reached) / doubling scheme — needs value reasoning | open |
| 2026-09-20 | detector | tier2: 7 files pied-piper MiniMeToken clones (`0x1829…`, `0x744d…`, `0x765f…`, `0x93ed…`, `0xb9e7…`, `0xcbea…`, `0xd4c4…`) | B | Uncertain/Malicious via EXIT_GLOBAL_SWITCH/LEAK_PRIV_SWEEP | placement | `mapping(address=>Checkpoint[])` ledger not bound; Tier 3 `lido_ldo_minime` is the benign twin | open |
| 2026-09-20 | detector | tier2: `injected_DestroyToken_2`, `_15` | B | Malicious via OWN_HIDDEN_ROLE | dataset | injected `balance_test`/`totalSupply` ledger sits beside a real `balanceOf` that binds first; binding extra co-moving ledgers would flag staking/slash shapes | open (not forced) |
| 2026-09-20 | detector | tier2: `real_0x814f…` Dragon | B | Malicious via EXIT_SELL_ONLY | dataset | privileged burner burns its own balance (self-burn) | recorded |
| 2026-09-20 | detector | tier2: crpwarner `0x90F7…` PldGenesisRewardPool, `0x9372…` BoredBunny | B (Hidden Mint) | Uncertain / Malicious | external gate / placement | reward token minted outside the file; ERC721 `giftMint` ledger not modeled | open |
| 2026-09-20 | detector | tier2: crpwarner `0xD217…` BaseToken `require(enabled \|\| hasRole(...))` | transfer unprivileged | `transfer` marked privileged | policy | `bool_needed_true` treats `\|\|` operands as needed-true. Spec amended: disjunction yields no needed-true atoms; `state_gate \|\| auth_atom` on a transfer path is a gate with a privileged bypass (`priv_bypass`) | open |

Tier 0 — 2026-09-20 02:36, the organizers' public set arrived (`challenge_public/`, 5 files, labels in file comments; README + schema vendored at `docs/judge/challenge_public/`). Ingested with `bench ingest-discord`; `bench run detector --tier 0`: weighted 0.8, 3/5 exact. Under the organizers' own scoring (+1/0/−1) that is 3 of 5 points.

| date | tool | case | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | detector | tier0/P4_CappedMint | Benign | Uncertain(med_findings) BAL_PRIV_MINT `constant_cap`→MED | doctrine → **policy** | organizers' rule 2 (code-enforced cap is BENIGN) is explicit; spec "Judge alignment" table: `constant_cap` → INFO; also `ungate_exists`/`constant_floor`/`bounded_window` (symmetric restriction, rule 1) and `no_custody` (rule 3) → INFO. Superseded 02:55 by **decisive mode** (owner-ratified): bounding discriminators → INFO → Benign; Phase 5 `wave-policy-decisive` | fixed (Run 6: Benign, `constant_cap` → INFO, tier0 5/5) |
| 2026-09-20 | detector | tier0/P5_DelegatecallBackdoor | Malicious | Uncertain(slither_high) | placement | `STRUCT_DELEGATECALL_SETTABLE` required a PW state-var target; here the target is a **parameter** of an `onlyOwner` function (`execute(address target, bytes data)`), only the Slither `controlled-delegatecall` overlay caught it. Spec: trigger widened to parameter/calldata-dependent targets of privileged functions | fixed (Malicious, `STRUCT_DELEGATECALL_SETTABLE` HIGH at `execute` line 37; Tier 3 scan 0 hits) |
| 2026-09-20 | detector | tier0/* (output contract) | one JSON array on stdout, `MALICIOUS/BENIGN/UNCERTAIN`, `reasons[]`, `evidence[{function,line}]`, basename `file`, top-level files only, 10-min kill | `results.json` + `summary.md` at a path, `Malicious/…`, findings list | reporting | spec: `cli.py` submission mode + `run.sh` + `docker_entry`; `judge.schema.json` vendored; global budget (DT-13) | fixed (Run 6: `run.sh` → JSON array on stdout, judge-schema valid on the public set, DT-13) |
| 2026-09-20 | detector | tier0/P1, P2, P3 | Benign / Malicious / Malicious | Benign / Malicious / Malicious, evidence lines 32 & 37 on P3 match the README's stated basis | — | none | ok |

Run 5 — 2026-09-20 03:05, three parallel waves (policy: role provenance / two-step handoff / `eq_self` / blocking polarity / `||` + `priv_bypass` / parametrised sweep; compile: project layouts / dependency exclusion / BOM; E-rule: parameter-target `delegatecall` + Tier 0 labels). Merged tree 371 tests pass. Tier 1+3 weighted 0.8835 → **0.8932**, determinism pass, zero HIGH-FP, compile_fail 1 (intentional). Tier 0 3/5 → **4/5** (P4 still `Uncertain(med_findings)`, closed by Phase 5 decisive mode). 72 of the 74 previously-run Tier 1+3 verdicts unchanged; the two flips are the intended `oz_ownable_rug` → Malicious and `timelock_self_call` → Uncertain.

| date | tool | case | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | detector | tier1/_harness/trading_switch_owner_bypass (new) | Malicious (EXIT_GLOBAL_SWITCH, `priv_bypass`) | — | policy | needed-true(A‖B) = needed-true(A) ∩ needed-true(B); `state_gate \|\| auth_atom` on the transfer path fires the gate rule with `priv_bypass` (owner exempt), which blocks `ungate_exists` | fixed (Malicious 1.0) |
| 2026-09-20 | detector | tier2/crpwarner/0x11CBC781… (DHE) | Malicious (EXIT_ADDR_GATE) | Benign (`_transfer` also writes the map → not exclusively privileged) | placement | `privileged_blocking_writable`: a gate qualifies if any privileged writer can store the blocking polarity, even with an unprivileged writer | fixed (Malicious) |
| 2026-09-20 | detector | tier2/crpwarner/0xD217Dc0c… (BaseToken) | Malicious; `transfer` not privileged | Malicious; `transfer` read as privileged via `require(a \|\| hasRole)` | policy | `||` operands are not needed-true; verdict unchanged, PRIV_ROLE evidence corrected | fixed |
| 2026-09-20 | detector | tier2/honeybadger/0x9c0c5a14… (EtherToTheMoon) | Malicious/Uncertain (LEAK_PRIV_SWEEP) | Benign (owner `withdraw(amount)` amount was a parameter, not `address(this).balance`) | placement | `LEAK_PRIV_SWEEP` also fires when the privileged send amount is a parameter/state value not bound to the caller's ledger, sender-keyed mapping, or same-call `msg.value` | fixed (Malicious) |
| 2026-09-20 | detector | tier1/_harness/oz_accesscontrol_blacklist, PRIV_ROLE/ben | Benign (acc. Uncertain) | Uncertain (`managed_role` MED after `library_role` removal) | policy | `managed_role` implements AccessControl role-admin indirection (callee returns a non-bool field of the same mapping); skips internal helpers and sender-slot-only writers (`renounceRole`) | ok (Uncertain; decisive mode makes the re-shaped `PRIV_ROLE/ben` Benign and the blacklist harness Malicious — see Phase 5) |
| 2026-09-20 | detector | probe: `remappings.txt` / `foundry.toml` remappings | compiles | compile_failed | compile | parse both (first prefix wins, txt over toml); project prefix wins over the vendored `@openzeppelin/` | fixed |
| 2026-09-20 | detector | probe: nested relative import `../../shared/I.sol` | compiles | compile_failed | compile | allow-paths covers the whole input root | fixed |

Run 6 — 2026-09-20 05:20, **decisive mode** (Phase 5: policy + labels + scoring + submission waves, tree `1d944b2`, 951 tests). Tier 0+1+3 `--repeat 2`: weighted **0.9875**, `tier0_exact 5/5`, Tier 1 67/67 exact (rule recall 1.0), Tier 3 zero HIGH-FP, determinism pass. Tier 2 (779): weighted 0.8209 → **0.8909**; family recall 0.917 → 0.9146 (A 0.978 / B 0.867 / C 1.0 / F 0.910); on the 529 compiling Malicious-labelled files 373/141/13 → **503 Malicious / 8 Uncertain / 18 Benign**. Uncertain reasons are only `compile_failed` (189) and `external_dependency` (6) — DT-15 holds on every tier. HoneyBadger technique hits unchanged from Run 4 (hidden_state_update 138/158, others identical). Cross-check: the teammate's `noexit` at `16fac68` (52 rule ids) on the same corpus: Tier 0+1+3 0.9457 (tier0 5/5), Tier 2 0.7875 (family recall 0.674; 48 Benign on compiling Malicious-labelled files).

| date | tool | case | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | bench | tier2 first attempt: 426 consecutive `Uncertain(analysis_error)` from case 354 on | verdicts | worker `FileNotFoundError` | harness | a concurrent `bench run detector --tier 0/1/3 --repeat 2` (03:40–03:42) re-staged the shared `.bench_work/detector/run0/input` and deleted the Tier 2 files under the running iteration. Detector behaviour is correct (missing input → `analysis_error` abstain); the hazard is the fixed per-tool work dir. Rerun with no concurrent bench: 0 `analysis_error` | recorded (bench: per-invocation work dir or lock — owner call) |
| 2026-09-20 | detector | tier2: 63 files labelled `non-compiling paper fixture` (`accepted: [Uncertain]`) | Uncertain | Malicious (60) / Benign (3) — the compile ladder (relaxed pragma / BOM / SPDX rungs) rescues them; HIGH rules on them: LEAK_PRIV_SWEEP 40, EXIT_ADDR_GATE 36, BAL_PRIV_MINT 32, HONEYPOT_LEGACY 22, STRUCT_SELFDESTRUCT 17 | labels | ingest-time compile check used only the pinned solc, so the label forbids the paper's own verdict on a rescued compile. Costs 63/779 ≈ 0.08 of the Tier 2 mean; tool-neutral (`noexit` also gives 89 Malicious on these). Proposed label amend: `accepted: [Uncertain, <paper verdict>]` for the non-compiling rows — needs owner ratification, not applied | declined 2026-09-20 (owner): labels stay Uncertain-only (see Run 8 row) |
| 2026-09-20 | detector | tier2: 8 pied-piper `real_0x27054b…`, `0x6d3475…`, `0x7e0d05…`, `0x8bcb64…`, `0xa66daa…`, `0xda2e0a…`, `0xdf859c…`, `0x6e8b6f…` (Disable/Freeze) | Malicious | Benign (Run 4: Uncertain `med_findings`) | dataset (judge rule 1) | every one is a symmetric `pause`/`unpause` or `disableTransfers`/`enableTransfers` pair (`EXIT_GLOBAL_SWITCH` with `ungate_exists`, no `priv_bypass`) → INFO under decisive mode; the organizers' README calls a symmetric pause BENIGN, the pied-piper label calls it Disable/Freeze. `noexit` also says Benign on all 8 | recorded |
| 2026-09-20 | detector | tier2: 7 crpwarner (`0x51C5…`, `0x8275…`, `0x94b7…`, `0xAAf8…`, `0xEc4C…`, `0xf0b6…`, `0x9372…` BoredBunny), `hidden_state_update_0x07ec…`, `named_TwelHourTrains`, `injected_FreezeAccount_8` | Malicious | Benign | dataset / placement | unchanged from Run 4/5 rows above (plain OZ ERC-20s, dead flag, bare SWC sample, voted sweep, time-weighted Ponzi, ERC721 gift-mint ledger). `noexit` flags two of them: `0x9372…` as "transferFrom ignores allowances" (an ERC721 — wrong basis) and `TwelHourTrains` as "buy() debits the caller but pays the owner" | open (as before) |
| 2026-09-20 | detector | tier2: 6 `external_dependency` on Malicious-labelled files (`0xEe45…`, MiniMe clones `0x765f…`, `0x93ed…`, `0xb9e7…`, `0xcbea…`, `0xd4c4…`) | Malicious | Uncertain | doctrine | `doTransfer` calls a settable `controller`; the one analytic abstain left in decisive mode. The Tier 3 twin `lido_ldo_minime` is labelled Malicious/[Malicious, Uncertain] for the same shape | recorded |
| 2026-09-20 | detector | tier2 `--repeat 2`: 6 files (`0x186E…`, `0xcb6C…`, `0x8314…`, `0x9dB8…`, `uninitialised_struct_0xe6f2…`, `real_0x2467…`) | byte-identical runs (DT-1) | verdicts stable, findings drift: `FEE_UNBOUNDED` impact function / surviving writer finding, `OWN_HIDDEN_ROLE` var (`owner` vs `owner2`), `OWN_FAKE_RENOUNCE` join order, `SLITHER_HIGH_OVERLAY` instance | reporting (hash-order pick) | rules-layer `[0]`/`join` picks sorted by source line then name; overlay candidates sorted before dedup; regression `test_tier2_results_byte_identical_across_hash_seeds` (fails on 3 seeds pre-fix). Rerun: 0 verdict / family-hit flips on 779, determinism pass | fixed (`6138ea3`) |
| 2026-09-20 | detector | Docker all-tier (`--network none`, `/input:ro`, `--repeat 2`, 12m36s): 48 Tier 2 files `Uncertain(analysis_error)` that are Malicious (35) / `compile_failed` (13) locally — e.g. crpwarner `0x0414…` (`pragma solidity 0.8.19;`) | same verdict as from a writable dir (DT-13) | `[Errno 30] Read-only file system: /input/<stem>.__relaxed__.sol` — the retry ladder writes its rewritten copy next to the source; the `finally` unlink then raises `EROFS` (not `FileNotFoundError`), masking the `CompileError` as `analysis_error` | packaging (judge mount) | spec amended: ladder rewrites go to a scratch mirror of the input root under the system temp dir (hard links / copies, never symlinks; rewritten file overwrites its own relative path so imports and remappings resolve); parent-owned `DETECTOR_SCRATCH_DIR`, removed after the run and after a timeout kill; cleanup paths swallow `OSError`. A judge file with an exact recent pragma would otherwise score 0 | fixed (`6aee5fb`; Docker `:ro` repro → `0x0414…` `compile_failed`, `0x108D…` + `pragma 0.8.19` file Malicious; local T2 0.8909 with 0 `analysis_error`, ~4 min) |
| 2026-09-20 | noexit (teammate, `16fac68`) | tier1/{EXIT_AMOUNT_LIMIT,EXIT_GLOBAL_SWITCH,EXIT_TIME_GATE}/mal → Benign; `_harness/timelock_self_call` → Malicious (HIGH-FP, "owner reassigned in `acceptAdmin`"); `STRUCT_EXTERNAL_GATE/mal` → Malicious (label Uncertain); `usdc_fiattoken` → Benign | — | — | cross-check | informational for the teammate: the three plain exit gates are the simplest Track 1 shapes; the two-step handoff false positive is the same shape our `two_step_handoff` discriminator fixed. Their 7 new industry-scanner rules (`CANNOT_SELL_ALL`, `COOLDOWN_GATE`, `BUY_BLOCK`, `EOA_ONLY_GATE`, `PERSONAL_FEE`, `PHANTOM_TRANSFER`, `GAS_ABUSE`) add no Tier 2 recall over ours; `PHANTOM_TRANSFER` (emits `Transfer`, moves no balance) and asymmetric `EOA_ONLY_GATE` are shapes our catalog does not name — candidate catalog additions for the owner, not applied | recorded |

Probe — 2026-09-20, adversarial inputs and project layouts (`/tmp`, not corpus). Found outside the labelled corpus, fixed via spec amendments and harness fixtures:

| date | tool | case id | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | detector | probe: `is ERC20, Ownable` + `onlyOwner` blacklist gate (`OzOwnableRug`, now `_harness/oz_ownable_rug`) | Malicious | Uncertain(med_findings) | policy | `library_role` downgraded EXIT_ADDR_GATE because `onlyOwner` is vendored. Provenance is not a benign signal; `library_role` removed from the downgrade table (spec amended) | fixed (Malicious, EXIT_ADDR_GATE HIGH) |
| 2026-09-20 | detector | probe: OZ `mocks/compound/CompTimelock.sol` | Benign/Uncertain | Malicious (OWN_FAKE_RENOUNCE, OWN_REASSIGN_NONSTD) | policy | `acceptAdmin` (pending → admin handoff) read as fake renounce + nonstandard reassign; `setPendingAdmin` gated by `msg.sender == address(this)` read as unprivileged. Spec: `two_step_handoff` drop discriminator, `eq_self` auth atom; fixtures `_harness/timelock_self_call`, `_harness/oz_ownable2step_token` | fixed (Uncertain, no D-family HIGH; DROP applied before the concealment check) |
| 2026-09-20 | detector | probe: Foundry layout, `import "../lib/oz/…"` | compiles | Uncertain(compile_failed) | compile | `--allow-paths` only covered the file's parent; spec: allow the whole input root, honour `remappings.txt`/`foundry.toml`, auto-map `node_modules/` and `lib/<pkg>/` | fixed (allow-paths = CLI root; `remappings.txt` > `foundry.toml` > auto-map > vendored OZ) |
| 2026-09-20 | detector | probe: Foundry/Hardhat layout, 263 vendored OZ files | not analysed as targets | 187 compile_failed + 75 Benign + 1 Malicious rows | reporting | dependency packages (`node_modules/`, package-shaped `lib/<pkg>/`) excluded from targets, count reported | fixed (`analyze_dir` returns `(results, skipped)`; summary header line; 263 skipped on the probe) |
| 2026-09-20 | detector | probe: UTF-8 BOM prefix | compiles | Uncertain(compile_failed) | compile | BOM strip rung in the retry ladder | fixed (`note` contains `BOM stripped`) |
| 2026-09-20 | detector | probe: CRLF, empty file, comment-only, interface-only, library-only, nested dirs, paths with spaces, 3000-state-var file, infinite assembly loop, non-.sol files, two pragmas, wide pragma | sane | sane (no crash, no hang, 5 s total) | — | none needed | ok |
| 2026-09-20 | detector | probe: `address o;` (internal, no getter) gating a setter of an unused mapping | ? | Malicious (OWN_HIDDEN_ROLE) | doctrine | consistent with `tier1/OWN_HIDDEN_ROLE/mal` (hidden role with no consumer is labelled Malicious). Whether a hidden role with **no impact** should be MED is a doctrine question for the owner; not changed | question |
| 2026-09-19 | detector | tier1/{BAL_PRIV_MINT,EXIT_AMOUNT_LIMIT,EXIT_GLOBAL_SWITCH,PRIV_ROLE}/ben | Benign (acc. Uncertain) | Uncertain(med_findings) | doctrine | capped mint / bounded window / unpause exists / documented blacklist role → MED by design. **Reopened 2026-09-20** under decisive mode: bounded controls → INFO → Benign (organizers' rules 1–3); `PRIV_ROLE/ben` fixture re-shaped (Phase 5 `labels-amend`) | fixed (Run 6: all four Benign 1.0) |
| 2026-09-19 | detector | tier3/{usdc_fiattoken,oz_erc20_pausable_ownable,oz_erc20capped_accesscontrol,reflection_token,bancor_smarttoken} | Benign (acc. Uncertain) | Uncertain(med_findings) | doctrine | disclosed/managed privilege (blacklister, pauser, minter cap, issuer) → MED; no HIGH survived. **Reopened 2026-09-20** under decisive mode: bounded (`oz_pausable`, `oz_capped`, `reflection`) → Benign; governance-only (`usdc`, `bancor`) → Malicious, label amended to preferred Malicious / accepted [Malicious, Benign] | fixed (Run 6: bounded three Benign; `usdc`, `bancor` Malicious, zero HIGH-FP on Tier 3) |
| 2026-09-19 | detector | tier3/lido_ldo_minime | Benign (acc. Uncertain) | Uncertain(external_dependency) | doctrine | `doTransfer` calls settable `controller` → STRUCT_EXTERNAL_GATE MED, as spec'd. Under decisive mode `external_dependency` stays a legitimate abstain, but Lido also carries uncapped `generateTokens` under the controller role → governance-only → Malicious; label amended to preferred Malicious / accepted [Malicious, Benign] | fixed (Run 6: Uncertain `external_dependency`, accepted) |

Run 7 — 2026-09-20 14:45, **judge-run hardening** (plan `judge-run_hardening`, Waves A/B/C, tree `7a64a1b`, image `e0a089bddd3a`, 1018 tests incl. the Docker matrix). All tiers Docker `--network none --repeat 2 --timeout 1200`: weighted **0.9714**, `tier0_exact 5/5`, Tier 1 **71/71** exact (the four OZ v4/v5 harness fixtures added and on-label), Tier 2 0.8909 / family recall 0.9146, Tier 3 zero HIGH-FP, determinism pass, `Uncertain` reasons `compile_failed` (190) and `external_dependency` (8) only, 0 `analysis_error`. Per-file diff against the Run 6 image: no verdict changed. Between the waves one Tier 2 file (`0x0b1Ff525…`) briefly flipped to `compile_failed` — the dense pin set had moved 696/864 `pick_solc` results under the old "lowest satisfying" rule and picked 0.6.6 for a `^0.6.0` file that needs `calldata` on a public function (0.6.9+); the rule is now "highest patch in the lowest satisfying minor" (Wave B4), which restores every anchor pick and only moves exact / tight pragmas to a newly native pin. Runtime: 10.2 min per repeat with the host under load from another session (Run 6: 6.4 min idle); a same-file A/B of this tree against `799361b` on 71 Tier 2 files is 67 s vs 60 s (+12 %, the local-root cap resolver and zero-parameter closure), identical verdicts.

Probe — 2026-09-20 12:45, judge-environment packaging (plan `judge-run_hardening`; probes run against `trust404/detector:latest` built from `799361b` with the five Tier 0 files; not corpus). The judge README fixes the contract — offline, one JSON array on stdout, exit 0, 2 vCPU / 4 GB / 10 min — but says nothing about uid, rootfs, or how the image reaches the grader, so every plausible variant was tried:

| date | tool | case id | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | detector | probe: `docker run --user 1000:1000 … /input:ro` | 5/5 array | empty stdout (whole submission 0): `FileNotFoundError: '/.solc-select/artifacts'` raised at `from detector import cli` — `solc_select.solc_select` mkdirs `$HOME/.solc-select/artifacts` at import and Docker sets `HOME=/` for a uid the image does not know; `compile.py` also hardcoded `Path.home()/.solc-select` | packaging (uid) | Dockerfile bakes `ENV HOME=/opt/detector-home DETECTOR_SOLC_ARTIFACTS=/opt/detector-home/.solc-select/artifacts`, installs solc there, `chmod -R a+rX`; `compile.solc_artifact_roots()` resolves `$DETECTOR_SOLC_ARTIFACTS` → `solc_select.constants.ARTIFACTS_DIR` → `~/.solc-select/artifacts`; `tests/detector/test_docker_hardening.py` matrix (root / uid 1000 / uid 65534 + `--read-only --tmpfs /tmp --cap-drop ALL --security-opt no-new-privileges --memory 4g --cpus 2 --pids-limit 512`) asserts 5/5 and byte-identical stdout | fixed (Wave A; matrix 7/7 on `wip-a`) |
| 2026-09-20 | detector | probe: `--read-only` rootfs without `--tmpfs /tmp` | valid array | `[]` — `scratch_session()` called `tempfile.mkdtemp` unguarded; `run_submission` wraps the whole loop in it, so the `OSError` unwound to `_run_submission_mode`, which printed an empty array (whole submission 0) | packaging (rootfs) | `scratch_session()` logs once and yields `None`; rewrite rungs skip with `no scratch: <error>` in the attempt log; first-try-compiling files still score, ladder-dependent files degrade to `UNCERTAIN(compile_failed)` instead of the run dying (`Relax.sol` probe: `UNCERTAIN` without tmpfs, `BENIGN` with) | fixed (Wave A) |
| 2026-09-20 | detector | probe: bench-mode auto-detect on a writable `/output` mount | stdout array when the judge asks for it | `results.json` in `/output`, nothing on stdout — the only local image is bench-shaped and `detector/README.md` labelled that form "the judging form" | packaging (mode) | `DETECTOR_MODE=submission|bench` env override in `docker_entry.py` (auto-detect stays the fallback); `run.sh` passes `-e DETECTOR_MODE=submission`; README now leads with the submission form and marks the `/output` form "bench mode, not for judging" | fixed (Wave A + Wave C) |
| 2026-09-20 | detector | probe: cold clone, `./run.sh ./cases` with no image and no venv | either a valid array or a loud failure | `python3 -m detector.cli` → `ModuleNotFoundError: slither` → empty stdout, exit 1, no guidance; root README never mentioned `run.sh` | packaging (run path) | `run.sh`: `DETECTOR_IMAGE` (then `ghcr.io/sdh2222/trust404-detector:latest` if present locally, never pulls), `DETECTOR_PYTHON`, `import slither` preflight → `exit 2` with the three setup options on stderr and nothing on stdout; `backend=` line on stderr; `scripts/judge_smoke.sh` reproduces the judge flow with schema validation; root README "judges start here" section; `scripts/setup_local.sh` for the venv path (Wave B) | fixed (Wave C; `tests/detector/test_run_sh.py`) |
| 2026-09-20 | detector | probe: local image is `linux/arm64` (built on the Mac; colima has no `buildx`) | amd64 image the grader can run by digest | arm64 only; nothing published — a digest-frozen submission of this image would `exec format error` on the amd64 grader | packaging (arch) | `.github/workflows/detector-image.yml`: native amd64 build → `ghcr.io/<owner>/trust404-detector:<tag>,latest`, pull back by digest, hardened smoke matrix with `cmp` byte-identity, `docker save` tarball as release asset + artifact, digest in the job summary; root README records digest + tarball | fixed (Wave C; `submission-rc1` run `35492675636` green on native amd64 in 3m55s — digest `sha256:947d6960…ca9dc`, tarball sha256 `fc897f1f…7393`, package public, root README updated) |
| 2026-09-20 | detector | probe: exact pragma inside a gap of the pin set (`0.8.0`–`0.8.19`, `0.4.24/25`, `0.5.16`, `0.6.6`) | native compile, `note is None` | rescued only by the relax rung (`pragma 0.8.19 relaxed; compiled with 0.8.20`) — one rewrite away from `compile_failed` under a read-only rootfs with no tmpfs | compile (pins) | `detector/solc_versions.txt` is the single source of truth (46 pins: 0.4.24–0.4.26, 0.5.16–0.5.17, 0.6.6, 0.6.12, 0.7.6, 0.8.0–0.8.37); Dockerfile and `scripts/setup_local.sh` read it; `pick_solc` unchanged (`>=0.4.22 <0.6.0` → 0.4.24); a test asserts the Dockerfile has no hand-written version | fixed (Wave B; `test_compile.py` pick table + `test_installed_solc_matches_versions_file`) |
| 2026-09-20 | detector | probe: OpenZeppelin v5 project (`Ownable(msg.sender)` ctor, `utils/Pausable.sol`) | compiles, scored | `compile_failed` — only OZ 4.9.6 vendored, so every 2024+ judge-style token would score 0 | compile (deps) | OZ 5.7.0 `contracts/` vendored as `vendor/openzeppelin-contracts-v5` (no `mocks/`); `rank_oz_trees` orders trees by how many `@openzeppelin/contracts/<rel>` specs resolve (v4 first on ties), the whole ladder runs per tree, and an OZ-version rung bumps to the newest 0.8.x when the error names a file under the current tree (`oz=v5; compiled with 0.8.37`); `bench validate` tries v4 then v5 (`oz=v4`/`oz=v5` in the log) | fixed (Wave B; four `_harness/oz_v*` fixtures; validate 71/71 Tier 1, 863/863 all) |
| 2026-09-20 | detector | tier1/_harness/oz_v5_unbounded_mint (P2 shape on OZ v5) | Malicious (`BAL_PRIV_MINT`) | **Benign, 0 findings** — a −1 on the judge scale, not an abstain | analysis (auth) | `is_revert_ir` held `"revert "` in `REVERT_NAMES` as an intended prefix but tested exact membership, so Slither's `SolidityCustomRevert` (`revert OwnableUnauthorizedAccount(address)`) was never a revert arm → v5 `_checkOwner`'s `if (owner() != _msgSender()) revert …` was not an end-auth node → `is_privileged(mint)` False. Every 0.8.4+ `if (…) revert CustomError()` gate had the same hole (three Tier 2 crpwarner files use custom errors). Fix: match `SolidityCustomRevert` instances and the `"revert "` prefix | fixed (Wave B2; `test_privilege.py` custom-error gate + non-reverting `if` control; unbounded → Malicious `BAL_PRIV_MINT`) |
| 2026-09-20 | detector | tier1/_harness/oz_v5_capped_mint (P4 shape on OZ v5) | Benign (`constant_cap`) | **Malicious** — `BAL_PRIV_MINT` HIGH without `constant_cap`, plus `LEAK_ARBITRARY_TRANSFERFROM` HIGH | analysis (bound + dead path) | (1) `is_constant_bound`/`_reads_supply` looked only inside the condition node; v5 `ERC20Capped._update` splits `maxSupply = cap(); supply = totalSupply(); if (supply > maxSupply) revert` across nodes. Fix: resolve condition operands through same-function local definitions to state vars / constants / calls (a privileged-writable bound through a local still disqualifies). (2) v5's unified `ERC20._update` has a `_balances[from]` debit in the `from != 0` arm; `_mint` binds `from` to `address(0)`, so the debit is dead on the mint path but the rule counted it. Fix: `closure_with_zero_params` — a debit whose key parameter is the zero address on every internal-call path from the entry is not a holder debit. Controls: mutable cap through a local stays HIGH; owner `seize(from, …)` through the same `_update` still fires | fixed (Wave B3; capped → Benign `BAL_PRIV_MINT` INFO `constant_cap`; unbounded keeps `BAL_PRIV_MINT` only; OZ v4/v5 `seize` probes both Malicious with `LEAK_ARBITRARY_TRANSFERFROM`) |
| 2026-09-20 | detector | probe: OZ v5 token with owner `burnFrom(from, amount)` / `seize(from, amount)` | Malicious with burn/leak reasons | Malicious (correct) but `BAL_PRIV_MINT` also listed — the `_totalSupply += value` arm of `_update` is only dead via `_burn`'s `if (account == address(0)) revert` guard, which the path-insensitive closure cannot see | reasons quality | none yet; verdict is right, the extra rule id is noise in `reasons`. A path-sensitive prune (revert-guarded arms on the caller's constant arguments) would remove it | open (low; reasons only) |

Run 8 — 2026-09-20 17:31, **ensemble** (plan `ensemble_re-roll`, PRs #4 / #5 / #6, tree `96364bc`, image `trust404/ensemble:latest` `fca45895dff7` built `FROM trust404/detector:latest` `e0a089bddd3a`; rule: MALICIOUS if either engine, BENIGN only from detector, else UNCERTAIN). All tiers Docker `--network none --repeat 2 --timeout 3600`: weighted **0.9756** (detector Run 7: 0.9714), `tier0_exact 5/5`, Tier 3 **1.0** (Run 7: 0.9688 — Lido's `Uncertain(external_dependency)` becomes Malicious through noexit), Tier 2 family recall 0.9223 (0.9146) and uncertain rate 0.2234 (0.2503), zero HIGH-FP on Tier 1/3, determinism pass, `Uncertain` reasons `compile_failed` (175) only. 25 verdicts differ from detector, all `→ Malicious`, none `→ Benign`: 21 Tier 2 `compile_failed → Malicious` (noexit reads the AST without solc), 2 Tier 2 detector misses `Benign → Malicious` (`crpwarner/0x9372b371…`, `honeybadger/named_TwelHourTrains`), `tier1/STRUCT_EXTERNAL_GATE/mal` and `tier3/lido_ldo_minime` `Uncertain(external_dependency) → Malicious`. The 6 Tier 2 files where noexit alone says Benign on a labelled-malicious file are neutralised by the asymmetric rule (detector's verdict carries). **EN-5 is not met as written**: Tier 1 0.9859 (< 1.0) and Tier 2 mean 0.8819 (< 0.8909) because 14 of the 25 deltas land on labels that accept `Uncertain` only — 13 Tier 2 `non-compiling paper fixture` cases and the Tier 1 twin — so the ensemble's Malicious scores 0 there although the source datasets label every one of them malicious. That is a label convention from the compile-only era, not an ensemble bug; the rows below record it as a label decision for the owner (`docs/specs/baybench.md` Corpus tiers, spec-scope rule: humans amend). Judge-cap timing (`--cpus 2 --memory 4g --pids-limit 512 --network none`, 71 compiling Tier 2 files, arm64 host under qemu): ensemble 132 s (detector 132 s ∥ noexit 34 s) vs detector-only image 127 s, identical verdicts except `0x9372b371…` (the recovered miss), no Traceback — the second engine costs ~4 % wall clock. Owner decision 2026-09-20: the twin label is amended (this PR), the 250 non-compiling labels stay Uncertain-only; post-relabel numbers are recorded as Run 9 below by the orchestrator.

| date | tool | case id | expected | got | bucket | fix | status |
|---|---|---|---|---|---|---|---|
| 2026-09-20 | ensemble | tier2 13 non-compiling paper fixtures (`crpwarner/0x0414D8C8…`, `0x5946FE65…`, `0xA0ffC741…`, `0xF19308F9…`, `0xba751BFf…`; `pied-piper/real_0x174bfa66…`, `0x1d52a344…`, `0x41b6f689…`, `0x8ec7740a…`, `0x9ec8d44a…`, `0xa8ba86e4…`, `0xa8f5e53c…`, `0xdd8a513d…`) | label `Uncertain` / accepted `[Uncertain]` | `Malicious` via noexit (`EXIT_ADDR_GATE`, `BAL_PRIV_MINT`, `OWN_FAKE_RENOUNCE`, …) — scores 0.0 each, was 1.0 for detector's `compile_failed` | label (convention) | the 250 `non-compiling paper fixture` labels were written when every tool needed solc; their source datasets (CRPWarner, Pied-Piper) label the files malicious. Proposed amendment, all 250 at once (no cherry-picking): `preferred: Malicious, accepted: [Malicious, Uncertain]` — a no-compile engine's correct answer scores 1.0, a compile-only tool's abstain 0.75 (detector's weighted score drops accordingly; that is the honest number). Needs the owner's amend of `docs/specs/baybench.md` Corpus tiers (Tier 2) and BB-11 wording | declined 2026-09-20 (owner): labels stay Uncertain-only; detector 60 / ensemble 73 of these files score 0 for a correct Malicious |
| 2026-09-20 | ensemble | tier1/STRUCT_EXTERNAL_GATE/mal | label `Uncertain` / accepted `[Uncertain]` (spec: "stays Uncertain") | `Malicious` via noexit `EXTERNAL_TRANSFER_HOOK: Transfer logic delegated to an owner-replaceable external contract` (`_transfer` l.34, `setGuard` l.25); detector `Uncertain(external_dependency)` | label (doctrine) | same shape as `tier3/lido_ldo_minime`, whose label already reads `preferred: Malicious, accepted: [Malicious, Uncertain]` ("`Uncertain(external_dependency)` is the sanctioned abstain"). Proposed: align the Tier 1 twin to that — organizers' rule "실행 경로를 배포 전에 차단하거나 경고해야 하면 MALICIOUS" covers an owner-swappable transfer hook. Needs a `labels.yaml` amendment + `docs/specs/baybench.md` line 131 ("`STRUCT_EXTERNAL_GATE/mal` stays Uncertain") | fixed (label amended 2026-09-20, PR labels/struct-external-gate-twin: preferred Malicious, accepted [Malicious, Uncertain]; baybench.md Tier 1 bullet amended) |
| 2026-09-20 | ensemble | tier2/crpwarner/0x9372b371…, tier2/honeybadger/named_TwelHourTrains | Malicious | detector `Benign`, noexit `Malicious` → ensemble `Malicious` (1.0, was 0.0) | recall (covered by second engine) | none needed for the ensemble; detector-side root cause stays on the detector backlog | fixed (ensemble) |
| 2026-09-20 | ensemble | tier2 8 `compile_failed` files labelled `preferred: Malicious` (`crpwarner/0xEe45E37e…`, `honeybadger/hidden_state_update_0x95be2203…`, `pied-piper/real_0x765f0c16…`, `0x93ed3fbe…`, `0xb9e7f856…`, `0xcbeaec69…`, `0xd4c435f5…`, `0xe1aee984…`) | Malicious | detector `Uncertain(compile_failed)` 0.5 → ensemble `Malicious` 1.0 | recall (compile) | none needed; these are the cases the second engine exists for | fixed (ensemble) |
| 2026-09-20 | ensemble | tier3/lido_ldo_minime | Malicious (acc. Uncertain) | detector `Uncertain(external_dependency)` 0.75 → ensemble `Malicious` 1.0 | recall | none | fixed (ensemble) |

Run 9 — 2026-09-20 19:13 (ensemble) / 19:42 (detector), **post-relabel rescoring** (plan `ship_ensemble_rc2`, PR #9: `STRUCT_EXTERNAL_GATE/mal` twin → preferred Malicious, accepted [Malicious, Uncertain]; the 250 non-compiling labels stay Uncertain-only). Code tree `a53c690` (#5 + #7 merged; no engine change since Run 7/8); images rebuilt from it: `trust404/detector:latest` `cd5539f6b9de`, `trust404/ensemble:latest` `f888fd9077c8` `FROM` it. All tiers Docker `--network none --repeat 2 --timeout 3600`, the two runs sequential. Ensemble: weighted **0.9803** (Run 8: 0.9756), `tier0_exact 5/5`, Tier 1 **1.0** (71/71 exact), Tier 2 0.8819 (family recall 0.9223, uncertain 0.2234), Tier 3 1.0, determinism pass, HIGH-FP 0 on Tier 1/3, `Uncertain` reasons `compile_failed` (175) only, p50 974 s per all-tier run (arm64 host under qemu). Detector: weighted 0.9703 (Run 7: 0.9714 — the whole difference is the twin, whose `Uncertain(external_dependency)` now scores 0.75 against preferred Malicious; Tier 1 exact 70/71), Tier 2 0.8909, Tier 3 0.9688, determinism pass, no verdict moved vs Run 7 (`compile_failed` 190, `external_dependency` 8, 0 `analysis_error`), p50 618 s. **EN-5 as amended is met**: on the 529 Malicious-labelled Tier 2 files the ensemble gives Malicious 513 / Benign-miss 16 / Uncertain 0 (mean 0.9698) against the detector's 503 / 18 / 8 (0.9584), and every tier mean is ≥ the detector's on decisive labels (Tier 0 1.0 = 1.0, Tier 1 1.0 ≥ 0.9965, Tier 2 decisive 0.9698 ≥ 0.9584, Tier 3 1.0 ≥ 0.9688). The full Tier 2 mean stays below the detector's (0.8819 < 0.8909) only through the 250 Uncertain-only labels: the ensemble scores 0 on 76 of them (73 Malicious + 3 Benign) against the detector's 63 (60 + 3) — the declined decision recorded above, not a regression. 25 verdict deltas detector → ensemble, all `→ Malicious`, the same set as Run 8. Cross-engine Benign check (read-only cross-tab of the 11:02 noexit run vs detector Run 7, 859 common cases): where Benign is not an accepted verdict, detector says Benign on 21 files and noexit on 78 (overlap 19; noexit-only 56 Tier 2 + the three plain exit-gate twins); on the 42 Benign-labelled cases detector 42/42, noexit 41/42 with no case noexit alone gets — so a symmetric "Benign if either" rule would add 16 wrong Benigns where the detector abstains and no Benign recall; a Benign-veto rule (both engines) is the candidate worth measuring after rc2 (issue #8).

Source of the gap list: `coverage.tool_gaps` / `coverage.format_gaps_md`. Tier 3 rows drive verdict-policy tuning (false positives); Tier 0/1/2 rows drive recall.
